{
  "id": "crmTgEventsV1",
  "name": "CRM project events → Telegram",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "project-events",
        "options": {}
      },
      "id": "node-webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [
        240,
        304
      ],
      "webhookId": "crm-project-events"
    },
    {
      "parameters": {
        "jsCode": "// Формирует текст Telegram-сообщения из payload outbox.\n// Webhook-node кладёт POST body в $json.body — берём его как корень.\n// Если event_type не из списка — возвращает пусто и Telegram skip'ается.\n// app_base_url приходит из cfg.AppBaseURL воркера, link на конкретный\n// проект — /manager/projects/{id} (не /admin/board?p=...).\nconst raw = $input.first().json;\nconst e = raw.body || raw;\nconst t = e.event_type;\nconst d = e.data || {};\n\n// Markdown-escape для динамических полей (заголовок, имя, контакт).\nconst esc = (s) => String(s ?? '').replace(/([_*`])/g, '\\\\$1');\nconst title = esc(d.title || '(без названия)');\nconst clientName = esc(d.client_name || '—');\nconst clientContact = esc(d.client_contact || '');\nconst spec = esc(d.specialist_display_name || '—');\nconst budget = d.budget ? `${d.budget} ₽` : '';\nconst reason = esc(d.reason || '');\n\nconst projectId = d.project_id || e.aggregate_id;\nconst app = e.app_base_url || '';\nconst link = (projectId && app)\n  ? `\\n[Открыть в CRM](${app}/manager/projects/${projectId})`\n  : '';\n\nconst fmtClient = clientContact ? `${clientName} (${clientContact})` : clientName;\n\nlet text;\nswitch (t) {\n  case 'project.created':\n    text = `🆕 *Новый бриф*\\n*${title}*` +\n           `\\nКлиент: ${fmtClient}` +\n           (budget ? `\\nБюджет: ${budget}` : '') +\n           link;\n    break;\n  case 'project.specialist_assigned':\n    text = `👤 *Назначен специалист*\\n*${title}*` +\n           `\\nКлиент: ${fmtClient}` +\n           `\\nСпециалист: ${spec}` +\n           link;\n    break;\n  case 'project.specialist_approved':\n    text = `✅ *Менеджер одобрил специалиста*\\n*${title}*` +\n           `\\nСпециалист: ${spec}` +\n           link;\n    break;\n  case 'project.assigned':\n    text = `👨‍💼 *Менеджер взял в работу*\\n*${title}*` +\n           `\\nКлиент: ${fmtClient}` +\n           (d.manager_display_name ? `\\nМенеджер: ${esc(d.manager_display_name)}` : '') +\n           link;\n    break;\n  case 'project.comment_added': {\n    const body = esc(d.body || '').slice(0, 500);\n    text = `💬 *Новый комментарий*\\n*${title}*` +\n           `\\n${body}` +\n           link;\n    break;\n  }\n  case 'moderation.specialist_pending': {\n    // Спец встал в очередь модерации (publish_requested — впервые/после reject;\n    // content_changed — был approved, поменял профиль). См.\n    // docs/SPECIALIST_MODERATION.md. Ссылка ведёт на админку.\n    const userId = d.user_id || e.aggregate_id;\n    const displayName = esc(d.display_name || '—');\n    const email = esc(d.email || '');\n    const why = d.reason === 'content_changed'\n      ? 'изменения после одобрения'\n      : 'первая публикация / повторная попытка';\n    const modLink = (userId && app)\n      ? `\\n[Открыть карточку](${app}/admin/moderation/${userId})`\n      : '';\n    text = `🔍 *Новая заявка на модерацию*\\n*${displayName}*` +\n           (email ? `\\n${email}` : '') +\n           `\\nПричина: ${why}` +\n           modLink;\n    break;\n  }\n  default:\n    return [];\n}\nreturn [{ json: { text } }];"
      },
      "id": "node-format",
      "name": "Format message",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        464,
        304
      ]
    },
    {
      "parameters": {
        "chatId": "-1003414312576",
        "text": "={{$json.text}}",
        "additionalFields": {
          "disable_web_page_preview": true,
          "parse_mode": "Markdown",
          "message_thread_id": 22
        }
      },
      "id": "node-telegram",
      "name": "Telegram",
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [
        688,
        304
      ],
      "webhookId": "6a21c82a-614a-49df-9124-062f995d7660",
      "credentials": {
        "telegramApi": {
          "name": "Telegram account"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Format message",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format message": {
      "main": [
        [
          {
            "node": "Telegram",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate"
  }
}
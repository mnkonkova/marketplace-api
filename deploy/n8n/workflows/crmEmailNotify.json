{
  "id": "crmEmailNotify",
  "name": "CRM email notifications (verify + reset)",
  "active": false,
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "email-events",
        "responseMode": "onReceived",
        "options": {}
      },
      "id": "node-webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [240, 304],
      "webhookId": "crm-email-events"
    },
    {
      "parameters": {
        "jsCode": "// Принимает outbox event {event_id, event_type, data:{to,to_name,token,base_url}}\n// и собирает поля для SMTP Send Email node (reg.ru, noreply@).\n// Поддержка: email.verify_send, email.password_reset_send.\nconst raw = $input.first().json;\nconst e = raw.body || raw;\nconst t = e.event_type;\nconst d = e.data || {};\n\nconst to     = d.to || '';\nconst toName = d.to_name || '';\nconst token  = d.token || '';\nconst base   = (d.base_url || '').replace(/\\/$/,'');\n\nlet subject, plain, html;\nswitch (t) {\n  case 'email.verify_send': {\n    const link = `${base}/verify?token=${encodeURIComponent(token)}`;\n    subject = 'Подтвердите email';\n    plain = `Здравствуйте${toName?', '+toName:''}!\\n\\nПодтвердите свой email перейдя по ссылке:\\n${link}\\n\\nСсылка действительна 24 часа.\\n\\n— wayprmarket`;\n    html  = `<p>Здравствуйте${toName?', '+toName:''}!</p>` +\n            `<p>Подтвердите свой email:</p>` +\n            `<p><a href=\"${link}\">Подтвердить</a></p>` +\n            `<p>Если кнопка не работает — скопируйте ссылку: <br>${link}</p>` +\n            `<p>Ссылка действительна 24 часа.</p>` +\n            `<p>— wayprmarket</p>`;\n    break;\n  }\n  case 'email.password_reset_send': {\n    const link = `${base}/auth/reset?token=${encodeURIComponent(token)}`;\n    subject = 'Сброс пароля';\n    plain = `Здравствуйте${toName?', '+toName:''}!\\n\\nДля сброса пароля перейдите по ссылке:\\n${link}\\n\\nЕсли вы не запрашивали сброс — проигнорируйте это письмо.\\n\\n— wayprmarket`;\n    html  = `<p>Здравствуйте${toName?', '+toName:''}!</p>` +\n            `<p>Для сброса пароля:</p>` +\n            `<p><a href=\"${link}\">Сбросить пароль</a></p>` +\n            `<p>Если кнопка не работает — скопируйте ссылку: <br>${link}</p>` +\n            `<p>Если вы не запрашивали сброс — проигнорируйте это письмо.</p>` +\n            `<p>— wayprmarket</p>`;\n    break;\n  }\n  default:\n    return [];\n}\n\nreturn [{ json: { to, subject, plain, html } }];"
      },
      "id": "node-format",
      "name": "Render email",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [464, 304]
    },
    {
      "parameters": {
        "fromEmail": "noreply@wayprmarket.com",
        "toEmail": "={{$json.to}}",
        "subject": "={{$json.subject}}",
        "html": "={{$json.html}}",
        "text": "={{$json.plain}}",
        "options": {}
      },
      "id": "node-smtp",
      "name": "Send via reg.ru SMTP",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [688, 304],
      "credentials": {
        "smtp": {
          "name": "reg.ru SMTP noreply"
        }
      }
    }
  ],
  "connections": {
    "Webhook": {
      "main": [[{"node": "Render email", "type": "main", "index": 0}]]
    },
    "Render email": {
      "main": [[{"node": "Send via reg.ru SMTP", "type": "main", "index": 0}]]
    }
  },
  "settings": {"executionOrder": "v1"}
}
